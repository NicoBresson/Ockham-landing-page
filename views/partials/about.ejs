<!-- About -->
<section id="about">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading text-uppercase"><%= __('homepage.about.title') %></h2>
                <h3 class="section-subheading text-muted"><%= __('homepage.about.subtitle') %></h3>
            </div>
        </div>
        <div class="row">
        </div>
    </div>
</section>
